## Supabase Egress Reduction Plans

This is is future traffic starts getting to the database egress limits. 

 - I could split the ingestdb and obsaqidb into different accounts. 
 - I could split ingestdb into 2, into different accounts. 
 - Reduce the ingestdb retention days, so less data will be needed from supabase. 