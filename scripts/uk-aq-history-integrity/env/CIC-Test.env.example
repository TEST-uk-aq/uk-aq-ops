# UK-AQ History Integrity — CIC-Test environment.
#
# Copy this file to the deployed root as:
#   /Users/mikehinford/uk-aq-history-integrity/env/CIC-Test.env
# then edit the BACKFILL_* paths to point at the local CIC-Test backfill tooling.
#
# Legacy full profile reference for local tooling. The dispatcher reads only
# its external one-line repository selector, and the repository runner loads
# the selected repository root .env instead of this file.

UK_AQ_OPS_REPO_ROOT="/Users/mikehinford/Dropbox/Projects/UK-AQ Website & Network/TEST UK-AQ GH Repos/TEST-uk-aq-ops"
